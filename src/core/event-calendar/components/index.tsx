import { Box } from '@mantine/core';

import EventCalendar from './EventCalendar';

const Dashboard = () => {
    return (
        <Box>
            <EventCalendar />
        </Box>
    );
};

export default Dashboard;
